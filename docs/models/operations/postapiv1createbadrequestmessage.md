# PostApiV1CreateBadRequestMessage

## Example Usage

```typescript
import { PostApiV1CreateBadRequestMessage } from "@linkage-open/sdk/models/operations";

let value: PostApiV1CreateBadRequestMessage =
  "Workflow must have at least one node and one edge";
```

## Values

```typescript
"Missing required headers" | "Workflow must have at least one node and one edge"
```